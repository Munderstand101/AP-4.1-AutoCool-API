<form method="post" action="{{ path('facture_h_delete', {'id': facture_h.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facture_h.id) }}">
    <button class="btn">Delete</button>
</form>
