{% extends 'base.html.twig' %}

{% block title %}TrancheHoraire index{% endblock %}

{% block body %}
    <h1>TrancheHoraire index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Duree</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tranche_horaire in tranche_horaires %}
            <tr>
                <td>{{ tranche_horaire.id }}</td>
                <td>{{ tranche_horaire.duree ? tranche_horaire.duree|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('tranche_horaire_show', {'id': tranche_horaire.id}) }}">show</a>
                    <a href="{{ path('tranche_horaire_edit', {'id': tranche_horaire.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('tranche_horaire_new') }}">Create new</a>
{% endblock %}
