<form method="post" action="{{ path('tranche_km_delete', {'id': tranche_km.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tranche_km.id) }}">
    <button class="btn">Delete</button>
</form>
