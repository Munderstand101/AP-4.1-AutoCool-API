<form method="post" action="{{ path('facture_k_m_delete', {'id': facture_k_m.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facture_k_m.id) }}">
    <button class="btn">Delete</button>
</form>
